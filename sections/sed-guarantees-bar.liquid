<section class="bg-white border-y border-zinc-200">
  <div class="container mx-auto px-4 py-8 flex flex-wrap justify-center gap-10 md:gap-16">
    {% for block in section.blocks %}
      <div class="flex items-center gap-2.5" {{ block.shopify_attributes }}>
        <div class="[&>svg]:w-4 [&>svg]:h-4 [&>svg]:text-zinc-400">
          {{ block.settings.icon_svg }}
        </div>
        <span class="font-body text-[10px] uppercase tracking-[0.2em] text-zinc-500">{{ block.settings.text }}</span>
      </div>
    {% endfor %}
  </div>
</section>

{% schema %}
{
  "name": "SED Guarantees Bar",
  "settings": [
  ],
  "blocks": [
    {
      "type": "guarantee",
      "name": "Guarantee",
      "limit": 4,
      "settings": [
        { "type": "html", "id": "icon_svg", "label": "Icon SVG" },
        { "type": "text", "id": "text", "label": "Text" }
      ]
    }
  ],
  "presets": [
    { "name": "SED Guarantees Bar" }
  ]
}
{% endschema %}
